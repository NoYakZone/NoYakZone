<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">
    <insert id="register" resultType="com.sm_oss.NoYakZone.dto.UserDto" parameterType="int">
        INSERT INTO users (id, name, password, birth, phone, email, address, official)
        VALUES (#{id}, #{name}, #{password}, #{birth}, #{phone}, #{email}, #{address}, #{official})
    </insert>
</mapper>
